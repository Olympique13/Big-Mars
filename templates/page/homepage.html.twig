{% extends 'base.html.twig' %}

{% block title %}BigMars!
{% endblock %}

{% block body %}
	<p class="font-bold text-3xl">Bienvenue sur bigmars !</p>
	<a href="{{ path ('app_login') }}">
		se connecter</a>
	<a href="{{ path ('app_register') }}">
		s'inscrire</a>
	<a href="{{ path ('app_event') }}">Voir event</a>
	{% if is_granted('ROLE_ADMIN') %}
		<a href="{{ path ('admin') }}">
			Admin
		</a>
	{% endif %}
{% endblock %}
